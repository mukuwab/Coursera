n=5

while n > 0 : 
    print(n)
    n=n-1

# Loops (reapted steps) have iteration variables change
# each time though a loop
# will reevaluate the while loop as long as the condition is true

# iteration variable (n>0) keeps the loop from running forever
    # defines the exit condition

print('Blastoff!')

#sequential steps are not nested (indented)
#conditional and repeated code is nested

