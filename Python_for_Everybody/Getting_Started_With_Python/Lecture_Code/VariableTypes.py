#use the type method  to find out the type of variables

eee = 'hello '+'there'
# eee = eee + 1
    #this will result in a traceback error b/c you are attempting to combine
    #variables of two seperate types

type(eee) #str